#include "header.h"
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>

int main() {
	int pid;

	//TODO: creazione processo startPL
	pid = 
	if (pid == 0){

	} else if (pid < 0){
		printf("Error \n");
	}

	//TODO: creazione processo startRL
	pid = 
	if (pid == 0){

	} else if (pid < 0){
		printf("Error \n");
	}
	
	//TODO: attesa processi figlio
	
	return 0;
}	
